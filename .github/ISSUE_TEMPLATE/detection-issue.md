---
name: "Faux positif / faux négatif — False positive / false negative"
about: "Signaler une règle de détection qui se trompe (rules/sast, dast, iast, rasp, waf)"
title: "[rules] <slug> — <langage/language>"
labels: ["bug", "detection"]
---

<!-- FR -->
**Fichier de règle concerné :** `rules/sast/<lang>/<slug>.yaml` (ou dast/iast/rasp/waf)
**Type de problème :** faux positif / faux négatif
**Extrait de code déclenchant (ou ne déclenchant pas) la règle :**
```
<code>
```
**Comportement attendu :**

<!-- EN -->
**Relevant rule file:** `rules/sast/<lang>/<slug>.yaml` (or dast/iast/rasp/waf)
**Issue type:** false positive / false negative
**Code snippet triggering (or not triggering) the rule:**
```
<code>
```
**Expected behavior:**

---
Ne signale jamais un payload d'exploitation fonctionnel ici — décris le pattern, pas une attaque réelle.
Never include a functional exploit payload here — describe the pattern, not a real attack.
